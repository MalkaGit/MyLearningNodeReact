//Repo: step1
export class Person {
    public id: string;                                      //no capital letter
    public name: string;

    constructor(id: string,name: string) {
        this.id = id;
        this.name = name;
    }
}